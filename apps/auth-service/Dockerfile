# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @qcommerce/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t qcommerce-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system qcommerce-auth-service && \
          adduser --system -G qcommerce-auth-service qcommerce-auth-service

COPY apps/auth-service/dist qcommerce-auth-service/
COPY apps/auth-service/package.json qcommerce-auth-service/
RUN chown -R qcommerce-auth-service:qcommerce-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix qcommerce-auth-service --omit=dev -f install

CMD [ "node", "qcommerce-auth-service" ]
